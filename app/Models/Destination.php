<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Destination extends Model
{
    use HasFactory;

/*

   protected $connection = 'sqlite'; = En caso un modelo x se conecte a otra bd

   protected $table = 'name-table';
   protected $primaryKey = 'name-id'
   protected $incrementing = false; = Indica que la llave ya no sera autoincrement
   protected $keyType = 'string'; = Indica el tipo de dato de la llave primaria.
   protected $timestamps = false; = Indica que no se utilizaran los campos de fecha
   protected $dateFormat = 'U'; = Formato en que se guardara la fecha y se recupera

   En caso tengan otro nombre los campos de fechas declaramos contantes al inicio

   const CREATED_AT = 'name-campo';
   const UPDATED_AT = 'name-campo';

 */

}
